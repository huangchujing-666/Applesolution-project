@{
    ViewBag.Title = "TrueLoyalty® Club :: 登記成為會員";
}
    <div class="address"><a href="@Url.Content("~/Club/Member")" title="首頁">首頁</a> <img src="@Url.Content("~/Content/images/adderss_icon2.png")" width="10" height="8" alt=">" /> <a href="@Url.Content("~/Club")"><span>LOYALTY<sup>®</sup> Club</span></a> <img src="@Url.Content("~/Content/images/adderss_icon2.png")" width="10" height="8" alt=">" /> 登記專頁 </div>
  </div>
@*  <div class="club_banner" id="topbanner">
    @foreach (var item in Model.TopBanners)
    {
        if (item.link != "")
        {
    <div><a href="@item.link" title="@item.title"><img src="@Url.Content("~/materials/banner")/@item.file_path" width="983" height="263" alt="@item.title" border="0" /></a></div>
        }
        else
        {
    <div><img src="@Url.Content("~/materials/banner")/@item.file_path" width="983" height="263" alt="@item.title" border="0" /></div>
        }
    }
  </div>*@
<script type="text/javascript">
    $(function () {
        $('#topbanner').cycle({
            fx: 'fade'
        });
    });
</script>

  <div class="club_content">
    <div class="left">
     <h3>成功登記成為會員</h3>

     <p>恭喜您！您已成功登記成為LOYALTY 的會員。@*您同時會收到啟動電郵。*@</p>
     <p><a href="@Url.Content("../Club/Index")" title="返回主頁">返回主頁</a></p>
    </div>
    <div class="right">
      <h3>會員有何優惠</h3>
      所有LOYALTY產品內都隱藏着一組「產品密碼」，會員可於此網站登記密碼以賺取積分，累積滿一定分數，即可換取禮品，包括全線LOYALTY產品或其他禮品。LOYALTY，您的「產品密碼」，為您打開「獎賞之門」。 
    </div>
